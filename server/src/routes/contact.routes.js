import {Router} from 'express';import {Contact} from '../models/Content.js';import {requireAuth,requireRole} from '../middleware/auth.js';const r=Router();r.post('/',async(req,res)=>{try{const data=await Contact.create(req.body);res.status(201).json({success:true,data});}catch(e){res.status(400).json({success:false,message:e.message});}});r.get('/',requireAuth,requireRole('Admin','Editor'),async(_req,res)=>res.json({success:true,data:await Contact.find().sort({createdAt:-1})}));r.put('/:id',requireAuth,requireRole('Admin','Editor'),async(req,res)=>{const data=await Contact.findByIdAndUpdate(req.params.id,req.body,{new:true});if(!data)return res.status(404).json({success:false,message:'Inquiry not found'});res.json({success:true,data});});r.delete('/:id',requireAuth,requireRole('Admin'),async(req,res)=>{await Contact.findByIdAndDelete(req.params.id);res.json({success:true});});export default r;
