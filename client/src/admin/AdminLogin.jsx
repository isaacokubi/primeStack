import {useState} from 'react';import {api} from '../services/api.js';import {useNavigate} from 'react-router-dom';export default function AdminLogin(){const [form,setForm]=useState({email:'',password:''}),[error,setError]=useState(''),nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{await api.post('/auth/login',form);nav('/admin')}catch(e){setError(e.response?.data?.message||'Login failed')}};return <div className="pageHead"><span className="eyebrow">ADMIN</span><h1>Sign in</h1><form className="form" onSubmit={submit}><input type="email" required placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input type="password" required placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn primary">Sign in</button>{error&&<p className="error">{error}</p>}</form></div>}
